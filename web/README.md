# OSS Radar — Web (PWA)

React + Vite frontend for OSS Radar. See the [project root README](../README.md) for the full picture
(what this app is, how it talks to the API, run instructions, design decisions).

Quick start:

```bash
npm install
npm run dev -- --host
```
